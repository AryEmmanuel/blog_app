<div class="row center-xs corrige form-user white text-left">
  <div class="col-xs-8 col-md-6">
    <div class="box">
			<h2>¿Olvidaste tu contraseña?</h2>

			<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
			  <%= devise_error_messages! %>
				<p>Te enviaremos un correo donde te daremos instrucciones de cómo recuperar tu contraseña.</p>
			  <div class="field">
			    <%= f.label :email, "Escribe tu dirección de correo electrónico:" %><br />
			    <%= f.email_field :email, autofocus: true, class:"form-control" %>
			  </div>

			  <div class="actions">
			    <%= f.submit "Enviar", class:"submit" %>
			  </div>
			<% end %>

			<%= render "devise/shared/links" %>
		</div>
	</div>
</div>